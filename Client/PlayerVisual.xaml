<UserControl x:Class="JAKE.Client.PlayerVisual"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="50" Width="50">

    <Grid>
        <Rectangle x:Name="playerRectangle" Width="50" Height="50"/>
        <TextBlock Text="{Binding PlayerName, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="12"/>
    </Grid>
</UserControl>
