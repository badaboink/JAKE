<UserControl x:Class="JAKE.client.HealthBoostVisual"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JAKE.client"
             mc:Ignorable="d" 
              Height="20" Width="20">
    <Grid>
        <Ellipse x:Name="HealthBoostCircle" Width="{Binding EllipseSizeC}" Height="{Binding EllipseSizeC}"/>
    </Grid>
    <!--Height="{Binding HealthBoostImageHeight}" Width="{Binding HealthBoostImageWidth}">

    <Grid>
        <Image x:Name="HealthBoostImage"   Height="{Binding HealthBoostImageHeight}" Width="{Binding HealthBoostImageWidth}" Source="{Binding HealthBoostImageSource}" />
    </Grid>-->
</UserControl>
