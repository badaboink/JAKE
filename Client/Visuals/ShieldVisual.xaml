<UserControl x:Class="JAKE.client.ShieldVisual"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JAKE.client"
             mc:Ignorable="d" 
             Height="20" Width="20">
    <Grid>
        <Ellipse x:Name="shieldCircle" Width="{Binding EllipseSizeC}" Height="{Binding EllipseSizeC}"/>
    </Grid>
             <!--Height="{Binding ShieldImageHeight}" Width="{Binding ShieldImageWidth}">

    <Grid>
        <Image x:Name="shieldImage"   Height="{Binding ShieldImageHeight}" Width="{Binding ShieldImageWidth}" Source="{Binding ShieldImageSource}" />
    </Grid>-->
</UserControl>
